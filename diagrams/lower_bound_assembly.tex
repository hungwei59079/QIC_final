\begin{tikzpicture}[>={Stealth[length=4pt]},
  box/.style={draw, rounded corners=2pt, align=center,
    text width=3cm, inner sep=4pt, font=\footnotesize}]

% Row 1
\node[box] (claim) at (0, 0)
  {SIC-POVM claim\\[2pt]
   $\exists\,b$: overlap $\leq 0.99$};

\node[box] (norm) at (5.5, 0)
  {Normalization\\[2pt]
   $\|v_C\|^2 = 1 \pm 2^{-0.1n}$};

% Row 2
\node[box] (conc) at (0, -3)
  {Concentration\\[2pt]
   cross-term sum $\leq 5$};

\node[box] (tvlem) at (5.5, -3)
  {TV $\leq$ cross terms\\[2pt]
   $d_{\mathrm{TV}} \leq
    \tfrac{1}{N}\!\sum|\cdots|$};

% Row 3
\node[box] (percopy) at (2, -6)
  {Per-copy\\[2pt]
   $d_{\mathrm{TV}} \leq 2^{-\Omega(n)}$};

\node[box] (fid) at (8, -6)
  {Fidelity\\[2pt]
   $\leq 2^{-\Omega(n)}$};

% Row 4
\node[box] (coupling) at (2, -9)
  {Coupling: $2^{o(n)}$ copies\\[2pt]
   $d_{\mathrm{TV}} \leq 2^{-\omega(n)}$};

% Row 5
\node[box] (theorem) at (4.5, -12)
  {\textbf{Theorem:} $2^{\Omega(n)}$\\
   copies needed};

% Arrows
\draw[->] (claim) -- (conc);
\draw[->] (norm) -- (tvlem);
\draw[->] (norm) to[bend left=25] (fid);
\draw[->] (conc) -- (percopy);
\draw[->] (tvlem) -- (percopy);
\draw[->] (percopy) -- (coupling);
\draw[->] (coupling) -- (theorem);
\draw[->] (fid) -- (theorem);

\end{tikzpicture}
